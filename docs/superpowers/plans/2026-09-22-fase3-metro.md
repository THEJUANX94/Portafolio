# Fase 3 — Recorrido en metro Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Página `/[lang]/experiencia/` con la experiencia como mapa de metro vertical: líneas de colores por tipo (empleo, proyectos y hackatón, estudios), estaciones de lo más reciente a lo más antiguo, una estación abierta a la vez, todas las líneas confluyendo arriba en "¿Siguiente estación? Hablemos".

**Architecture:** El trazado (orden de estaciones, qué líneas existen, qué tramos de cada línea pasan por cada fila) se calcula en `src/lib/metro.ts`, función pura probada con Vitest. El render es un componente Astro sin JS: cada estación es un `<details name="metro">` nativo (acordeón exclusivo, accesible y funcional sin JavaScript) y los rieles son `div`s posicionados por fila, así se alinean aunque la tarjeta abierta cambie de alto. **Cambio respecto al spec/ADR 0004:** no se usa isla React; se retira React del proyecto (ADR 0005).

**Tech Stack:** Astro 7.3, Tailwind 4.3, Vitest 5, Playwright 1.63.

**Spec:** [2026-09-22-portafolio-v1-design.md](../specs/2026-09-22-portafolio-v1-design.md) — *Páginas → 2. Recorrido*.

**Estado previo:** Fases 1 y 2 en `main`.

**Datos:** hoy hay 3 estaciones (2 de empleo, 1 de proyectos). La línea de estudios aparece sola cuando se agreguen entradas `line: "education"` en `experience.json` (fase 6, falta la fecha de inicio en la UPTC). El trazado soporta 1 a 3 líneas.

---

## Mapa de archivos

```
frontend/
├── src/lib/metro.ts                     (crear: buildMetro)
├── src/styles/global.css                (modificar: colores de línea)
├── src/i18n/es.json, en.json            (modificar: textos del recorrido)
├── src/components/MetroMap.astro        (crear)
├── src/pages/[lang]/experiencia.astro   (crear)
├── astro.config.mjs, package.json, tsconfig.json   (modificar: quitar React)
├── tests/unit/metro.test.ts             (crear)
└── tests/e2e/journey.spec.ts            (crear)
docs/01-arquitectura/decisions/0005-metro-details-nativo-sin-react.md  (crear)
docs/01-arquitectura/decisions/0001-…, 0004-…    (modificar: estado)
docs/01-arquitectura/architecture.md, docs/log.md, CLAUDE.md, README.md  (modificar)
```

Comandos de `frontend/` con el directorio de trabajo en `Portfolio/frontend`.

---

### Task 1: Trazado del metro (`lib/metro.ts`)

**Files:**
- Create: `frontend/src/lib/metro.ts`
- Test: `frontend/tests/unit/metro.test.ts`

Modelo: fila 0 del render es la terminal "¿Siguiente estación?" (no la devuelve esta función). Las filas `0..n-1` que devuelve `buildMetro` son estaciones. Cada línea presente corre desde la terminal hasta su estación más antigua. En la fila `i`, para cada línea: `up` = el tramo entre el borde superior de la fila y el punto de la estación existe (`i <= última fila de la línea`); `down` = el tramo del punto al borde inferior existe (`i < última fila de la línea`).

- [ ] **Step 1: Test que falla**

`frontend/tests/unit/metro.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildMetro } from '../../src/lib/metro';

const e = (id: string, line: 'work' | 'projects' | 'education', start: string, end: string | null) => ({ id, line, start, end });

const hackathon = e('hackathon', 'projects', '2025-10', null);
const techLead = e('tech-lead', 'work', '2025-08', '2026-08');
const intern = e('intern', 'work', '2025-03', '2025-07');
const NOW = '2026-09';

describe('buildMetro — orden', () => {
  it('ordena de lo más reciente a lo más antiguo; vigente (null) usa "now"', () => {
    const { rows } = buildMetro([intern, hackathon, techLead], NOW);
    expect(rows.map((r) => r.entry.id)).toEqual(['hackathon', 'tech-lead', 'intern']);
  });
  it('empate en fin → inicio más reciente primero; luego id', () => {
    const a = e('a', 'work', '2024-01', '2025-01');
    const b = e('b', 'work', '2024-06', '2025-01');
    const c = e('c', 'projects', '2024-06', '2025-01');
    expect(buildMetro([a, c, b], NOW).rows.map((r) => r.entry.id)).toEqual(['b', 'c', 'a']);
  });
  it('no muta la entrada y conserva el objeto original', () => {
    const input = [intern, techLead];
    const { rows } = buildMetro(input, NOW);
    expect(input[0]).toBe(intern);
    expect(rows[0].entry).toBe(techLead);
  });
});

describe('buildMetro — líneas', () => {
  it('solo las líneas presentes, en orden canónico work → projects → education', () => {
    const edu = e('uptc', 'education', '2020-01', '2026-06');
    expect(buildMetro([edu, hackathon, techLead], NOW).lines).toEqual(['work', 'projects', 'education']);
    expect(buildMetro([hackathon], NOW).lines).toEqual(['projects']);
  });
  it('vacío → sin líneas ni filas', () => {
    expect(buildMetro([], NOW)).toEqual({ lines: [], rows: [] });
  });
});

describe('buildMetro — tramos por fila', () => {
  it('cada línea corre desde la terminal hasta su estación más antigua', () => {
    const { rows } = buildMetro([hackathon, techLead, intern], NOW);
    // fila 0: hackathon (projects) — work pasa de largo, projects termina aquí
    expect(rows[0].tracks).toEqual({
      work: { up: true, down: true },
      projects: { up: true, down: false },
      education: { up: false, down: false },
    });
    // fila 1: tech-lead — solo work
    expect(rows[1].tracks.work).toEqual({ up: true, down: true });
    expect(rows[1].tracks.projects).toEqual({ up: false, down: false });
    // fila 2: intern — work termina
    expect(rows[2].tracks.work).toEqual({ up: true, down: false });
  });
});
```

- [ ] **Step 2: Verificar que falla**

Run: `pnpm test`
Expected: FAIL — no se resuelve `../../src/lib/metro`.

- [ ] **Step 3: Implementación**

`frontend/src/lib/metro.ts`:
```ts
import { LINES } from './schemas';

export type Line = (typeof LINES)[number];

export interface MetroInput {
  id: string;
  line: Line;
  start: string;
  end: string | null;
}

export interface Track {
  /** Tramo desde el borde superior de la fila hasta el punto de la estación. */
  up: boolean;
  /** Tramo desde el punto de la estación hasta el borde inferior de la fila. */
  down: boolean;
}

export interface MetroRow<T extends MetroInput> {
  entry: T;
  tracks: Record<Line, Track>;
}

export interface MetroLayout<T extends MetroInput> {
  lines: Line[];
  rows: MetroRow<T>[];
}

/**
 * Ordena estaciones (reciente → antigua; `end: null` cuenta como `now`) y calcula,
 * por fila, qué tramos de cada línea se dibujan. Todas las líneas nacen en la
 * terminal superior y terminan en su estación más antigua.
 */
export function buildMetro<T extends MetroInput>(entries: readonly T[], now: string): MetroLayout<T> {
  const endKey = (e: T) => e.end ?? now;
  const sorted = [...entries].sort(
    (a, b) =>
      endKey(b).localeCompare(endKey(a)) || b.start.localeCompare(a.start) || a.id.localeCompare(b.id),
  );

  const lines = LINES.filter((line) => sorted.some((e) => e.line === line));
  const lastRow = new Map<Line, number>();
  sorted.forEach((e, i) => lastRow.set(e.line, i));

  const rows = sorted.map((entry, i) => {
    const tracks = {} as Record<Line, Track>;
    for (const line of LINES) {
      const last = lastRow.get(line);
      tracks[line] = last === undefined ? { up: false, down: false } : { up: i <= last, down: i < last };
    }
    return { entry, tracks };
  });

  return { lines, rows };
}
```

- [ ] **Step 4: Verificar que pasa**

Run: `pnpm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/lib/metro.ts frontend/tests/unit/metro.test.ts
git commit -m "feat: add metro layout builder"
```

---

### Task 2: Colores de línea y textos del recorrido

**Files:**
- Modify: `frontend/src/styles/global.css`, `frontend/src/i18n/es.json`, `frontend/src/i18n/en.json`

- [ ] **Step 1: Colores**

En `frontend/src/styles/global.css`:
- En `:root` (claro) agregar:
  ```css
  --line-work: #4c1d95;
  --line-projects: #a78bfa;
  --line-education: #a8a29e;
  ```
- En los dos bloques oscuros agregar:
  ```css
  --line-work: #c4b5fd;
  --line-projects: #7c3aed;
  --line-education: #78716c;
  ```
- En `@theme inline` agregar:
  ```css
  --color-line-work: var(--line-work);
  --color-line-projects: var(--line-projects);
  --color-line-education: var(--line-education);
  ```

- [ ] **Step 2: Textos**

Agregar a `frontend/src/i18n/es.json`:
```json
  "journey.title": "Mi recorrido",
  "journey.description": "Recorrido profesional de Juan Sebastián Martínez: empleo, proyectos y estudios en un mapa de metro.",
  "journey.intro": "Cada estación es una etapa. Toca una para ver qué hice ahí.",
  "journey.back": "Volver al perfil",
  "journey.next": "¿Siguiente estación?",
  "journey.nextCta": "Hablemos",
  "journey.legend": "Líneas",
  "journey.projects": "Proyectos de esta etapa",
  "line.work": "Empleo",
  "line.projects": "Proyectos y hackatón",
  "line.education": "Estudios"
```

Agregar a `frontend/src/i18n/en.json`:
```json
  "journey.title": "My journey",
  "journey.description": "Career journey of Juan Sebastián Martínez: work, projects and education as a metro map.",
  "journey.intro": "Each station is a stage. Tap one to see what I did there.",
  "journey.back": "Back to profile",
  "journey.next": "Next station?",
  "journey.nextCta": "Let's talk",
  "journey.legend": "Lines",
  "journey.projects": "Projects from this stage",
  "line.work": "Work",
  "line.projects": "Projects & hackathon",
  "line.education": "Education"
```

- [ ] **Step 3: Verificar**

Run: `pnpm test && pnpm check`
Expected: PASS (claves iguales en es/en), `0 errors`.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/styles/global.css frontend/src/i18n
git commit -m "feat: add metro line colors and journey UI strings"
```

---

### Task 3: Componente `MetroMap.astro`

Sin JS. Riel izquierdo con una columna de 1.5 rem por línea presente; el punto de cada estación se alinea con el centro del `<summary>` (`--dot: 1.75rem`, mitad de `min-h-14`).

**Files:**
- Create: `frontend/src/components/MetroMap.astro`

- [ ] **Step 1: Componente**

```astro
---
import type { Locale } from '../lib/i18n';
import type { Localized } from '../lib/schemas';
import type { Line, MetroLayout, MetroInput } from '../lib/metro';
import { formatRange, formatDuration, monthsBetween } from '../lib/dates';
import { t } from '../i18n';

export interface StationView extends MetroInput {
  role: Localized;
  organization: Localized;
  highlights: Localized[];
  tech: string[];
  projects: { id: string; title: Localized }[];
}

interface Props {
  lang: Locale;
  layout: MetroLayout<StationView>;
  now: string;
  contactHref: string;
}
const { lang, layout, now, contactHref } = Astro.props;
const { lines, rows } = layout;

const BG: Record<Line, string> = { work: 'bg-line-work', projects: 'bg-line-projects', education: 'bg-line-education' };
const BORDER: Record<Line, string> = { work: 'border-line-work', projects: 'border-line-projects', education: 'border-line-education' };
const col = (i: number) => `calc(${i} * 1.5rem + 0.75rem)`;
const railWidth = `${Math.max(lines.length, 1) * 1.5}rem`;
---
<div data-metro style={`--rail: ${railWidth}; --dot: 1.75rem;`}>
  <ul aria-label={t(lang, 'journey.legend')} class="mb-6 flex flex-wrap gap-2 text-sm" data-metro-legend>
    {
      lines.map((line) => (
        <li class="inline-flex items-center gap-2 rounded-full border border-border bg-surface px-3 py-1">
          <span aria-hidden="true" class:list={['h-1.5 w-5 rounded-full', BG[line]]} />
          {t(lang, `line.${line}`)}
        </li>
      ))
    }
  </ul>

  <ol class="relative">
    <!-- Terminal: todas las líneas confluyen aquí -->
    <li class="relative grid grid-cols-[var(--rail)_minmax(0,1fr)] gap-3 pb-4">
      <div aria-hidden="true" class="relative">
        {lines.length > 1 && (
          <span class="absolute h-1.5 rounded-full bg-line-work" style={`top: calc(var(--dot) - 0.1875rem); left: ${col(0)}; width: calc(${lines.length - 1} * 1.5rem);`} />
        )}
        {lines.map((line, i) => (
          <span class:list={['absolute w-1.5 -translate-x-1/2', BG[line]]} style={`left: ${col(i)}; top: var(--dot); bottom: 0;`} />
        ))}
        <span class="absolute size-5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-line-work ring-4 ring-bg" style={`left: ${col(0)}; top: var(--dot);`} />
      </div>
      <div class="flex min-h-14 flex-wrap items-center gap-x-3 gap-y-1">
        <span class="font-semibold">{t(lang, 'journey.next')}</span>
        <a href={contactHref} data-metro-next class="font-semibold text-primary underline underline-offset-4 hover:no-underline">
          {t(lang, 'journey.nextCta')}
        </a>
      </div>
    </li>

    {
      rows.map(({ entry, tracks }, rowIndex) => {
        const stationCol = lines.indexOf(entry.line);
        const months = monthsBetween(entry.start, entry.end ?? now);
        return (
          <li class="relative grid grid-cols-[var(--rail)_minmax(0,1fr)] gap-3 pb-4" data-station-row>
            <div aria-hidden="true" class="relative">
              {lines.map((line, i) => (
                <>
                  {tracks[line].up && (
                    <span class:list={['absolute top-0 w-1.5 -translate-x-1/2', BG[line]]} style={`left: ${col(i)}; height: var(--dot);`} />
                  )}
                  {tracks[line].down && (
                    <span class:list={['absolute bottom-0 w-1.5 -translate-x-1/2', BG[line]]} style={`left: ${col(i)}; top: var(--dot);`} />
                  )}
                </>
              ))}
              <span
                class:list={['absolute size-5 -translate-x-1/2 -translate-y-1/2 rounded-full border-4 bg-bg', BORDER[entry.line]]}
                style={`left: ${col(stationCol)}; top: var(--dot);`}
              />
            </div>

            <details
              name="metro"
              open={rowIndex === 0}
              data-station
              data-line={entry.line}
              class:list={['group rounded-xl border border-l-4 bg-surface open:shadow-sm', BORDER[entry.line]]}
            >
              <summary class="flex min-h-14 cursor-pointer list-none items-center gap-3 rounded-xl px-4 py-2 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary [&::-webkit-details-marker]:hidden">
                <span class="min-w-0 flex-1">
                  <span class="block font-semibold">{entry.role[lang]}</span>
                  <span class="block text-sm text-text-muted">
                    {entry.organization[lang]} · {formatRange(entry.start, entry.end, lang)}
                  </span>
                </span>
                <span aria-hidden="true" class="text-primary transition-transform group-open:rotate-180">▾</span>
              </summary>
              <div class="border-t border-border px-4 pb-4 pt-3" data-station-body>
                <p class="text-sm font-medium text-primary">
                  {t(lang, `line.${entry.line}`)} · {formatDuration(months, lang)}
                </p>
                <ul class="mt-3 list-disc space-y-1.5 pl-5">
                  {entry.highlights.map((h) => <li>{h[lang]}</li>)}
                </ul>
                {entry.tech.length > 0 && (
                  <ul class="mt-3 flex flex-wrap gap-1.5">
                    {entry.tech.map((tech) => <li class="rounded-full bg-surface-soft px-2 py-0.5 text-xs">{tech}</li>)}
                  </ul>
                )}
                {entry.projects.length > 0 && (
                  <div class="mt-4">
                    <p class="text-sm font-semibold">{t(lang, 'journey.projects')}</p>
                    <ul class="mt-1 space-y-1 text-sm">
                      {entry.projects.map((p) => (
                        <li>
                          <a href={`/${lang}/proyectos/${p.id}/`} class="text-primary underline underline-offset-4 hover:no-underline">
                            {p.title[lang]}
                          </a>
                        </li>
                      ))}
                    </ul>
                  </div>
                )}
              </div>
            </details>
          </li>
        );
      })
    }
  </ol>
</div>
```

`t(lang, \`line.${entry.line}\`)`: si `pnpm check` rechaza el template literal como `UiKey`, crear en el componente `const LINE_KEY = { work: 'line.work', projects: 'line.projects', education: 'line.education' } as const;` y usar `t(lang, LINE_KEY[line])`.

- [ ] **Step 2: Verificar**

Run: `pnpm check`
Expected: `0 errors`.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/MetroMap.astro
git commit -m "feat: add metro map component with native details stations"
```

---

### Task 4: Página `/[lang]/experiencia/`

**Files:**
- Create: `frontend/src/pages/[lang]/experiencia.astro`

- [ ] **Step 1: Página**

```astro
---
import { getCollection, getEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import LangSwitch from '../../components/LangSwitch.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import MetroMap, { type StationView } from '../../components/MetroMap.astro';
import { LOCALES, type Locale } from '../../lib/i18n';
import { buildMetro } from '../../lib/metro';
import { toYearMonth } from '../../lib/dates';
import { t } from '../../i18n';

export function getStaticPaths() {
  return LOCALES.map((lang) => ({ params: { lang } }));
}

const lang = Astro.params.lang as Locale;
const now = toYearMonth(new Date());

const profileEntry = await getEntry('profile', 'main');
if (!profileEntry) throw new Error('Falta la entrada "main" en src/data/profile.json');

const projectTitles = new Map((await getCollection('projects')).map((p) => [p.id, p.data.title]));

const stations: StationView[] = (await getCollection('experience')).map((e) => ({
  id: e.id,
  line: e.data.line,
  start: e.data.start,
  end: e.data.end,
  role: e.data.role,
  organization: e.data.organization,
  highlights: e.data.highlights,
  tech: e.data.tech,
  projects: e.data.projects.map((ref) => {
    const title = projectTitles.get(ref.id);
    if (!title) throw new Error(`experience "${e.id}" referencia el proyecto inexistente "${ref.id}"`);
    return { id: ref.id, title };
  }),
}));

const layout = buildMetro(stations, now);
---
<BaseLayout lang={lang} title={`${t(lang, 'journey.title')} — Juan Sebastián Martínez`} description={t(lang, 'journey.description')}>
  <div class="mx-auto max-w-3xl px-4 py-6 lg:py-10">
    <header class="flex items-center justify-between gap-2">
      <a href={`/${lang}/`} data-back-link class="font-semibold text-primary underline-offset-4 hover:underline">
        <span aria-hidden="true">← </span>{t(lang, 'journey.back')}
      </a>
      <div class="flex gap-2">
        <LangSwitch lang={lang} />
        <ThemeToggle lang={lang} />
      </div>
    </header>

    <main id="main" class="mt-8">
      <h1 class="text-3xl font-bold text-primary">{t(lang, 'journey.title')}</h1>
      <p class="mt-2 text-text-muted">{t(lang, 'journey.intro')}</p>
      <div class="mt-8">
        <MetroMap lang={lang} layout={layout} now={now} contactHref={`mailto:${profileEntry.data.email}`} />
      </div>
    </main>
  </div>
</BaseLayout>
```

Si Astro no permite exportar la interfaz `StationView` desde el frontmatter del `.astro`, moverla a `src/lib/metro.ts` (junto a `MetroInput`, importando `Localized` de `./schemas`) e importarla desde ahí en ambos archivos.

- [ ] **Step 2: Verificar**

Run: `pnpm check && pnpm build`
Expected: `0 errors`; build genera `dist/es/experiencia/index.html` y `dist/en/experiencia/index.html`.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages
git commit -m "feat: add journey page with metro map"
```

---

### Task 5: Pruebas de humo del recorrido

**Files:**
- Create: `frontend/tests/e2e/journey.spec.ts`

- [ ] **Step 1: Tests**

```ts
import { test, expect } from '@playwright/test';

test.describe('recorrido en español', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/es/experiencia/');
  });

  test('título, leyenda y estaciones en orden', async ({ page }) => {
    await expect(page.getByRole('heading', { level: 1 })).toHaveText('Mi recorrido');
    await expect(page.locator('[data-metro-legend] li')).toHaveText(['Empleo', 'Proyectos y hackatón']);
    const stations = page.locator('[data-station]');
    await expect(stations).toHaveCount(3);
    await expect(stations.nth(0)).toHaveAttribute('data-line', 'projects');
    await expect(stations.nth(1)).toContainText('Tech Lead');
    await expect(stations.nth(2)).toContainText('Practicante');
  });

  test('solo la estación más reciente está abierta al cargar', async ({ page }) => {
    const stations = page.locator('[data-station]');
    await expect(stations.nth(0)).toHaveAttribute('open', '');
    await expect(stations.nth(1)).not.toHaveAttribute('open', '');
    await expect(stations.nth(2)).not.toHaveAttribute('open', '');
  });

  test('con teclado se abre otra estación y la anterior se cierra', async ({ page }) => {
    const stations = page.locator('[data-station]');
    await stations.nth(1).locator('summary').focus();
    await page.keyboard.press('Enter');
    await expect(stations.nth(1)).toHaveAttribute('open', '');
    await expect(stations.nth(0)).not.toHaveAttribute('open', '');
    await expect(stations.nth(1).locator('[data-station-body]')).toContainText('57%');
  });

  test('enlaces: volver, contacto y proyectos de la etapa', async ({ page }) => {
    await expect(page.locator('[data-back-link]')).toHaveAttribute('href', '/es/');
    await expect(page.locator('[data-metro-next]')).toHaveAttribute('href', 'mailto:sebastianmn03@gmail.com');
    await expect(page.locator('[data-station]').nth(0).getByRole('link')).toHaveAttribute('href', '/es/proyectos/cardenas-vision/');
  });

  test('el selector de idioma conserva la página', async ({ page }) => {
    await page.locator('[data-lang-switch]').click();
    await expect(page).toHaveURL(/\/en\/experiencia\/$/);
    await expect(page.getByRole('heading', { level: 1 })).toHaveText('My journey');
  });
});

test('la principal enlaza al recorrido', async ({ page }) => {
  await page.goto('/es/');
  await page.locator('[data-journey-link]').click();
  await expect(page).toHaveURL(/\/es\/experiencia\/$/);
});

test('sin JavaScript las estaciones siguen siendo legibles y se pueden abrir', async ({ browser }) => {
  const context = await browser.newContext({ javaScriptEnabled: false });
  const page = await context.newPage();
  await page.goto('/es/experiencia/');
  await expect(page.locator('[data-station]').nth(0).locator('[data-station-body]')).toBeVisible();
  await page.locator('[data-station]').nth(2).locator('summary').click();
  await expect(page.locator('[data-station]').nth(2).locator('[data-station-body]')).toBeVisible();
  await context.close();
});

test('celular: sin scroll horizontal', async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 812 });
  await page.goto('/es/experiencia/');
  expect(await page.evaluate(() => document.documentElement.scrollWidth > window.innerWidth)).toBe(false);
});
```

- [ ] **Step 2: Correr**

Run: `pnpm test:e2e`
Expected: 23 passed (15 previos + 8 nuevos).

- [ ] **Step 3: Commit**

```bash
git add frontend/tests/e2e/journey.spec.ts
git commit -m "test: add journey page smoke tests"
```

---

### Task 6: Retirar React y documentar la decisión

**Files:**
- Modify: `frontend/astro.config.mjs`, `frontend/package.json` (vía pnpm), `frontend/tsconfig.json`
- Create: `docs/01-arquitectura/decisions/0005-metro-details-nativo-sin-react.md`
- Modify: `docs/01-arquitectura/decisions/0001-astro-sitio-estatico.md`, `docs/01-arquitectura/decisions/0004-metro-vertical-isla-react.md`, `docs/01-arquitectura/architecture.md`, `CLAUDE.md`, `README.md`

- [ ] **Step 1: Quitar dependencias**

Desde `frontend/`:
```bash
pnpm remove @astrojs/react react react-dom @types/react @types/react-dom
```

- [ ] **Step 2: Configuración**

En `frontend/astro.config.mjs`: borrar `import react from '@astrojs/react';` y la entrada `react(),` de `integrations`.

En `frontend/tsconfig.json`: borrar el bloque `"compilerOptions": { "jsx": "react-jsx", "jsxImportSource": "react" }` (y la coma previa), dejando:
```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
```

- [ ] **Step 3: Verificar**

Run: `pnpm check && pnpm test && pnpm build && pnpm test:e2e`
Expected: todo verde; 23 passed. `grep -rn "react" src astro.config.mjs tsconfig.json` sin resultados.

- [ ] **Step 4: ADR 0005**

`docs/01-arquitectura/decisions/0005-metro-details-nativo-sin-react.md`:
```markdown
---
type: decision
tags: [adr, ux, accesibilidad, experiencia]
---

# ADR 0005: Metro con `<details>` nativo, sin React

## Estado
Aceptado. Reemplaza el punto 3 de [ADR 0004](0004-metro-vertical-isla-react.md) y el punto 2 de [ADR 0001](0001-astro-sitio-estatico.md).

## Contexto

ADR 0004 preveía el metro como isla React. Al diseñar la fase 3, toda la interacción necesaria (abrir una estación, una abierta a la vez, teclado, lector de pantalla, funcionar sin JS) la cubre HTML nativo: `<details name="…">` es un acordeón exclusivo con `aria-expanded` implícito.

## Decisión

1. Cada estación es un `<details name="metro">`; la más reciente se renderiza con `open`.
2. El trazado (orden, líneas presentes, tramos por fila) se calcula en `src/lib/metro.ts` en build.
3. Los rieles son elementos posicionados por fila, así se alinean aunque cambie el alto de la tarjeta abierta.
4. Se retiran `@astrojs/react`, `react` y `react-dom`: no hay otro uso de React.

## Consecuencias

- **Positivas**: cero JavaScript en la página del recorrido; accesible y funcional sin JS; menos dependencias.
- **Costos aceptados**: la exclusividad (`name`) requiere navegadores de 2024 en adelante; en uno antiguo pueden quedar varias estaciones abiertas, sin romper nada. Si en el futuro se quiere una animación compleja, habría que reincorporar una isla.

Última actualización: 2026-09-22
```

- [ ] **Step 5: Actualizar documentos existentes**

- `docs/01-arquitectura/decisions/0004-metro-vertical-isla-react.md`: en `## Estado` poner `Aceptado. El punto 3 (isla React) fue reemplazado por [ADR 0005](0005-metro-details-nativo-sin-react.md).`
- `docs/01-arquitectura/decisions/0001-astro-sitio-estatico.md`: en `## Estado` poner `Aceptado. El punto 2 (React como isla) fue reemplazado por [ADR 0005](0005-metro-details-nativo-sin-react.md): hoy no hay React.`
- `docs/01-arquitectura/architecture.md`: en el diagrama mermaid reemplazar el nodo `Metro["components/metro/MetroMap.tsx (isla React — fase 3)"]` por `Metro["components/MetroMap.astro (details nativo)"]`; en la tabla agregar la fila `| \`src/lib/metro.ts\` | Trazado del metro: orden de estaciones, líneas presentes y tramos por fila. |` y en la sección 2 cambiar `Astro 7 + React 19 (isla) + Tailwind 4` por `Astro 7 + Tailwind 4`.
- `CLAUDE.md`: línea de stack `frontend/` → `Astro 7, Tailwind 4, zod 4 vía \`astro/zod\`. Sin React (ver ADR 0005).`
- `README.md`: fila Frontend → `Astro 7 + Tailwind 4 (sin JS de framework en el cliente)`.

- [ ] **Step 6: Commit**

```bash
git add frontend/astro.config.mjs frontend/package.json frontend/pnpm-lock.yaml frontend/tsconfig.json docs CLAUDE.md README.md
git commit -m "refactor: drop React in favor of native details for the metro"
```

---

### Task 7: Bitácora y verificación final

**Files:**
- Modify: `docs/log.md`

- [ ] **Step 1: Bitácora**

Agregar bajo `## 2026-09 — Diseño y base`:
```markdown
- **2026-09-22** — Fase 3 ([plan](superpowers/plans/2026-09-22-fase3-metro.md)): página `/[lang]/experiencia/` con el metro vertical (líneas por tipo, terminal "¿Siguiente estación? Hablemos", una estación abierta a la vez). Implementado con `<details>` nativo; se retira React ([ADR 0005](01-arquitectura/decisions/0005-metro-details-nativo-sin-react.md)). La línea de estudios aparecerá al agregar entradas `education` (falta la fecha de inicio en la UPTC).
```

- [ ] **Step 2: Verificación**

Desde `frontend/`: `pnpm check && pnpm test && pnpm build && pnpm test:e2e`
Expected: todo verde; 23 passed.

- [ ] **Step 3: Commit**

```bash
git add docs/log.md
git commit -m "docs: log phase 3 metro journey"
```
